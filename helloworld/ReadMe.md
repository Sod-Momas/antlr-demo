# antlr generate lexer tree

run `generateLexerTree.bat` on Windows Operation System, it will show a window.


the window of antlr
![window](generate-window.png)

generate tree:

![done](antlr4_parse_tree.png)
